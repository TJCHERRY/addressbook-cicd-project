pipeline {
    agent any

    stages {
        stage('Test GitHub SSH') {
            steps {
                sshagent(['github-jenkins-ssh']) {
                    sh 'ssh -T git@github.com || true'
                }
            }
        }
    }
}
